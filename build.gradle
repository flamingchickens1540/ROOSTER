task wrapper(type: Wrapper) {
    gradleVersion = '4.4'
}

subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }

    dependencies {
        compile 'org.jetbrains:annotations:15.0'
    }
}
